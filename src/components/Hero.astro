---
// src/components/Hero.astro
---

<section class="hero">
  <div class="container">
    <div class="hero-content">
      <div class="hero-text">
        <h1 class="hero-title">
          Türkiye'nin Güvenilir
          <span class="title-highlight">İç Oda Kapısı</span>
          Üreticileri Rehberi
        </h1>

        <p class="hero-description">
          Kaliteli PVC, laminoks, otel ve hastane kapıları. Türkiye geneli teslimat ve montaj hizmeti ile projelerinize değer katın.
        </p>

        <div class="hero-actions">
          <a href="#kategoriler" class="btn-primary">Kapı Kategorileri</a>
          <a href="/iletisim" class="btn-secondary">İletişime Geç</a>
        </div>
      </div>

      <div class="hero-visual">
        <div class="image-placeholder">
          <img
            src="/assets/img/hero/hero1-image.webp"
            alt="İç oda kapısı modelleri - PVC, laminoks ve ahşap kapı çeşitleri"
            width="500"
            height="600"
            loading="eager"
          />
        </div>
      </div>
    </div>


  </div>
</section>

<style>
  .hero {
    padding: 6rem 0 4rem;
    background: #ffffff;
    border-bottom: 1px solid #f3f4f6;
  }

  .hero-content {
    display: grid;
    grid-template-columns: 1fr;
    gap: 3rem;
    align-items: center;
    margin-bottom: 4rem;
  }

  .hero-title {
    font-size: 2.75rem;
    font-weight: 800;
    line-height: 1.2;
    color: #111827;
    margin-bottom: 1.5rem;
  }

  .title-highlight {
    color: #1f2937;
    position: relative;
  }

  .title-highlight::after {
    content: '';
    position: absolute;
    bottom: 2px;
    left: 0;
    right: 0;
    height: 3px;
    background: #f59e0b;
    border-radius: 2px;
  }

  .hero-description {
    font-size: 1.2rem;
    color: #6b7280;
    line-height: 1.7;
    margin-bottom: 2.5rem;
    max-width: 500px;
  }

  .hero-actions {
    display: flex;
    gap: 1rem;
    margin-bottom: 3rem;
    flex-wrap: wrap;
  }

  .btn-primary, .btn-secondary {
    padding: 1rem 2rem;
    border-radius: 8px;
    font-weight: 600;
    text-decoration: none;
    font-size: 1rem;
    transition: all 0.2s ease;
    border: none;
    cursor: pointer;
  }

  .btn-primary {
    background: #111827;
    color: white;
  }

  .btn-primary:hover {
    background: #1f2937;
    color: white;
  }

  .btn-secondary {
    background: transparent;
    color: #111827;
    border: 2px solid #e5e7eb;
  }

  .btn-secondary:hover {
    border-color: #d1d5db;
    background: #f9fafb;
  }

  .hero-features {
    display: flex;
    gap: 2rem;
    flex-wrap: wrap;
  }

  .feature-item {
    text-align: left;
  }

  .feature-item h3 {
    font-size: 1.5rem;
    font-weight: 800;
    color: #111827;
    margin-bottom: 0.25rem;
  }

  .feature-item p {
    font-size: 0.875rem;
    color: #6b7280;
    font-weight: 500;
  }

  .hero-visual {
    display: flex;
    justify-content: center;
    align-items: center;
  }

  .image-placeholder {
    width: 100%;
    max-width: 400px;
    height: 300px;
    background: #f9fafb;
    border: 2px dashed #d1d5db;
    border-radius: 12px;
    display: flex;
    align-items: center;
    justify-content: center;
    overflow: hidden;
  }

  .image-placeholder img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    border-radius: 8px;
  }

  .trust-indicators {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
    gap: 2rem;
    padding: 2rem 0;
    border-top: 1px solid #f3f4f6;
  }

  .indicator {
    text-align: center;
    padding: 1rem;
  }

  .indicator h4 {
    font-size: 1rem;
    font-weight: 600;
    color: #111827;
    margin-bottom: 0.5rem;
  }

  .indicator p {
    font-size: 0.875rem;
    color: #6b7280;
    margin: 0;
  }

  /* Responsive */
  @media (min-width: 768px) {
    .hero-content {
      grid-template-columns: 1fr 1fr;
      gap: 4rem;
    }

    .hero-title {
      font-size: 3.25rem;
    }

    .trust-indicators {
      grid-template-columns: repeat(4, 1fr);
    }
  }

  @media (max-width: 767px) {
    .hero {
      padding: 4rem 0 3rem;
    }

    .hero-title {
      font-size: 2.25rem;
    }

    .hero-description {
      font-size: 1.1rem;
    }

    .hero-actions {
      flex-direction: column;
    }

    .btn-primary, .btn-secondary {
      text-align: center;
    }

    .hero-features {
      justify-content: center;
      text-align: center;
    }

    .trust-indicators {
      grid-template-columns: repeat(2, 1fr);
    }
  }

  @media (min-width: 1024px) {
    .hero-title {
      font-size: 3.75rem;
    }

    .image-placeholder {
      max-width: 500px;
      height: 400px;
    }
  }
</style>